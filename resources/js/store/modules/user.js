import { USER_REQUEST, USER_ERROR, USER_SUCCESS } from "../actions/user";
// import apiCall from "/resources/js/utils/api.js";
import Vue from "vue";
import { AUTH_LOGOUT } from "../actions/auth";

const state = { 

};

const getters = {
  
};

const actions = {
  
};

const mutations = {
 
};

export default {
  state,
  getters,
  actions,
  mutations
};